stages:
  - build
  - deploy

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""
  IMAGE_NAME: bachxuanphong/gitlab-classroom
workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop" || $CI_COMMIT_BRANCH == "cicd"'
      when: always 
    - when: never
build-job:
  stage: build
  image: docker:24.0.5
  services:
    - name: docker:24.0.5-dind
      alias: docker

  before_script:
    - docker login -u $DOCKERHUB_USER -p $DOCKERHUB_PASSWORD
  script:
    - docker build -t $IMAGE_NAME:$CI_COMMIT_SHORT_SHA .
    - docker tag $IMAGE_NAME:$CI_COMMIT_SHORT_SHA $IMAGE_NAME:latest
    - docker push $IMAGE_NAME:$CI_COMMIT_SHORT_SHA
    - docker push $IMAGE_NAME:latest

  tags:
    - localhost1

deploy-job:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client

    # setup SSH key
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
    - chmod 600 ~/.ssh/id_ed25519

    # avoid host verification issue
    - ssh-keyscan -H $SERVER_IP >> ~/.ssh/known_hosts

  script:
    - echo "$STG_ENV" > .env
    - scp -P 45669 .env $SERVER_USER@$SERVER_IP:/opt/base-classroom/
    - |
      ssh -p 45669 $SERVER_USER@$SERVER_IP << EOF
        cd /opt/base-classroom/
        docker login -u $DOCKERHUB_USER -p $DOCKERHUB_PASSWORD
        docker compose restart
      EOF

  tags:
    - localhost1